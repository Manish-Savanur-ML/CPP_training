1. \d -- Match digit from 0-9 [0-9]

2. \D -- Match digit other that 0-9 [^0-9]

3. \w -- Match alphabet, digit [a-zA-Z0-9]

4. \W -- Matches other that alphabet, digit [^a-zA-Z0-9]

5. [] -- If anything mention in square braces will be matched as OR
ex -- [Cr]at This pattern will match Cat or rat

6. () -- If any pattern mention in parenthesis will be match as whole
ex -- (Cr)at This pattern will match Crat.

7. [^] -- This will match other then pattern specified in square braces
ex -- [^0-9] This will match other than digit

8. \s -- Include all special characters (\t, \r, \n, \c)

9. Check for period (.) in regex use \.

10 ^ $ -- Marks begining & end of string.
